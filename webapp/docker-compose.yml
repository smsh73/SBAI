services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - uploads:/app/uploads
      - outputs:/app/outputs
      - dbdata:/app/data

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NGINX_CONF: nginx.vm.conf
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  uploads:
  outputs:
  dbdata:
